<% provide(:title, 'New Lost Item') %>
<h1><%= t('app.text.new_item') %></h1>

<div class="row">
  <div class="span6 offset3">
    <%= render 'form' %>

    <%= link_to t('app.link.back'), user_path(@user) %>
  </div>
</div>
